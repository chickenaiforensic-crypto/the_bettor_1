# ERRATA-2026-08-03 — auditor compType corrections (AS RELAYED by the owner)

> **Paper-trail note (researcher, 2026-08-03):** the owner announced that the auditor issued
> `ERRATA-2026-08-03.md` and uploaded it to the repo. **No such file materialized in this
> sandbox** (checked repo root, `/home/user/uploads/`, and the full `origin/main` tree —
> nothing; the corrected WORKORDER-RPL text is likewise not on `origin/main`, which still
> carries the cb6e grammar line). The corrections below were therefore applied **verbatim
> from the owner's inline message**, which is treated as the authoritative relay. Please
> re-upload the file so this mirror can be replaced by the original document.

Relayed content (owner message, 2026-08-03):

1. **compType corrections.**
   - Affects the RPL league return: **Russian Relegation Playoff rows use compType `other`
     (not domestic-league); league rows stay `domestic-league`.**
   - **Cups = `domestic-cup` going forward.**
   - Re-read the corrected RPL workorder — fingerprint `cb6e` → **`9903cf856877d173ba71d72cef64e9c6`**.
2. **Standing from the cup audit:** **add `TEAM|FC Ufa`**, **write KAMAZ exactly**.

Researcher application (both returns rebuilt + full gate suites re-run 2026-08-03, sha256-stable):

- `handoffs/RPL-2021-2026_BP-TEAM-PACK_v2.txt`: 12 playoff rows switched to compType `other`
  (all other bytes unchanged); new `NOTE|info|comp_class`. Gates 69/69 PASS.
- `handoffs/RUSCUP-2021-2026_BP-TEAM-PACK_v2.txt`: all 189 rows now compType `domestic-cup`;
  `TEAM|FC Ufa|Russia|Russian Premier League|RPL|...` added (22 TEAM rows); `KAMAZ
  Naberezhnye Chelny` in exact form (was KamAZ); new `NOTE|info|comp_class`. Gates 162/162 PASS.
- Registered for upcoming returns: MOLCUP/KOSCUP/SCOCUP/SCOLC/USOC → `domestic-cup`;
  any future promotion/relegation-playoff rows → `other`.

---

## 2026-08-03 (evening) — auditor return clarification + owner upload announcement

- The auditor's return message clarifies the KAMAZ line beyond the original relay: **the roster
  string is written exactly `KAMAZ` on both in-scope rows** ("And still open from the cup: write
  exactly KAMAZ (2 rows)"). Applied to `handoffs/RUSCUP-2021-2026_BP-TEAM-PACK_v2.txt`
  (was `KAMAZ Naberezhnye Chelny`; full name kept in the identity NOTE). 162/162 gates green.
- Same message sanctions the CZ1 pro/rel block (12 rows, compType `other`) — applied;
  see `WORKORDER-STATUS.md` "Auditor return cycle".
- The owner announced "I will upload to you and you can place in the repo" together with an
  attachment of this ERRATA file. **At write time the attachment has not materialized in the
  sandbox** (`/home/user/uploads/` does not exist; repo tree and `origin/main` checked). When the
  file lands it will be placed at `supervisor/ERRATA-2026-08-03.md` and this mirror marked
  superseded-by-original.
