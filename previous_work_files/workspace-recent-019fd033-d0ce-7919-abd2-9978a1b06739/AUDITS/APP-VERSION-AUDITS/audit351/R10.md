# GATE EVIDENCE — R10: auditor D9/D10 fixes (v3.5.1)

**Engineer:** agent build · **Auditor:** system-side · **Status:** 🟢 both auditor repros fixed + pinned on the final file

Final file: `app/app-v3.5.1.html` · version **v3.5.1** · md5 `ce61de0f9e500d3917d053e9e4e77c3e` · 604,770 B
Backup: `backups/app-v3.5.1-ce61de0f.html` · deliver: `deliver/6/`

Auditor verdict on v3.5.0 (md5 `400077a9…`): **accepted as CURRENT** — replay hitRate fix and ingest commit-skip fix independently re-proven; store stable 1,436/539; seeds 9/0; R1/R4/D12 re-probes green. The two defects R9 *ordered* were the pair below.

---

## D9 — parseReturn league codes (requests.js)

**Repro (auditor, verbatim):** routine return updates EXISTING teams → `staged.identities` is empty → `codes: []` → the returned league section never flips.

**Root cause:** `parseReturn` collected league codes ONLY from `staged.identities` (new TEAM rows).

**Fix:** new `collectReturnCodes(store, staged)`:
1. codes from `staged.identities` (as before),
2. **plus** each staged match's home/away identity resolved in the store → `leagueCode` (existing teams),
3. **plus** competition-name → catalogue-code fallback (`leagueCodeFromCompetition`).

**Pin (`harness/acceptance-r10.js`, D9 — auditor repro verbatim: plain BP-TEAM-PACK v2, no SECTION line, FC Krasnodar v Akron Tolyatti both EXIST):**
```
PASS D9: return parses clean
PASS D9: codes inferred from existing teams (contains RPL, not [])
PASS D9: match stored (1436 -> 1437)
PASS D9: RPL section flips to partial
PASS D9: unrelated section stays requested
PASS D9: overall request state partial
```

## D10 — commitReturn unconditional success logging (ui.js)

**Repro (auditor, verbatim):** commit the SAME return a second time (all rows now duplicates). Actual: `data/return-commit | Central request return committed: 0 matches, 0 teams ().` + `snapshot/post-return` logged; with a fully-rejected block even the overall request state flips to "partial" with 0 rows stored.

**Fix:** `storedTotal = committed.matches + committed.teams`; when `0`:
- log **ONE** honest line `data/return-commit-skip`: *"Return committed nothing — N row(s) were duplicates or already present; no commit made."*
- NO `return-commit` success line · NO `snapshot/post-return` · NO request state / artifact change.
`ingest.commit` gained `silentLog` (the return path owns its logging) so exactly one line is written per action.

**Pin (`harness/acceptance-r10.js`, D10):**
```
PASS D10: second identical return stores 0 rows
PASS D10: exactly one honest skip log written
PASS D10: no NEW return-commit success line
PASS D10: no post-return snapshot stamped
PASS D10: request state untouched
PASS D10: exactly one log line added (the skip)
```

---

## Cosmetic corrections (auditor notes)
- v3.5.0 actual size **602,624 B** (my 602,425 was stale) — corrected in R9-D10.md + ZONES. Build now reports disk-true bytes.
- `SECTION|` lines are not in the documented return grammar; v3.5.0's block rejection of unknown rows is correct and stays.

## Full suite state (final file)
| Check | Result |
|---|---|
| `smoke_test_v3.js` | 49/49 |
| `acceptance-r8.js` (D12) | 13/13 |
| `acceptance-r9.js` (D9/D10 round 1) | 7/7 |
| `acceptance-r10.js` (R10 D9/D10) | **12/12** |
| `acceptance-r1-r4.js` | Krasnodar/Fakel ≤3 steps ✅ |
| `compare-legacy-engine.js` | 7/7 identical |
| `smoke_test.js` (legacy) | 156/156 |
| `grep -c "cdn-cgi\|challenge-platform"` | 0 |
| `grep -c "fitted fitted"` | 0 |
