# GATE EVIDENCE — R11: migration gate fixes M1–M3b (v3.5.2)

**Engineer:** agent build · **Auditor:** system-side · **Status:** 🟢 all four fixes + core gate green on the owner's REAL live-store export

Final file: `app/app-v3.5.2.html` · version **v3.5.2** · md5 `6bd76ae025fc6eee68e3186ac52ac5ec` · 609,411 B
Backup: `backups/app-v3.5.2-6bd76ae0.html` · deliver: `deliver/7/`
Source export: `data/seeds/pitch-rating-full-data-2026-08-02.json` (1,132,200 B · md5 `5a8ba49475acfa2340ce7fd66e4dfeb0` · format `pitch-rating-full` v1) — the owner's live store.

---

## M1 (P0) — store migration wired to the Data-tab intake ✅
`handleFiles` now sniffs JSON store exports **before** the pack path (`.json` name OR content starting `{`), via the new `stageUpload`:
- a JSON object that is a store export (`format==='pitch-rating-full'` | `format===1` | `app==='pitch-rating-full'` | has `identities`+`matches` | has `store`+`matches`) → `STORE.deserialize` → stages a **migration card** with the replace warning:
  `Store migration — 1432 matches · 792 teams · 86 venues · 215 sources · REPLACES the current store — export first if unsure`
- Approve → `commitMigration`: stamps fitted-model + dc-gate artifacts (exactly like boot), logs one `data/migration-commit`, saves, re-boots → store at 1,432/792.
- Discard → nothing. The old pack path is untouched (packs still stage pack cards).

**UI pin (auditor acceptance, `harness/acceptance-r11.js`):**
```
PASS M1: real export stages a MIGRATION card (not "Rejected")
PASS M1: staged card preview 1432/792
PASS M1: approve replaces the store (1432/792)
PASS M1: data/migration-commit logged
PASS M1: booted flow stamps fitted + gate artifacts
PASS M1/boot: gate artifact RPL+CZ1
PASS M1/boot: CSKA–Krylia fitted on migrated store
PASS M1: old pack path untouched (pack stages a pack card)
```

## M2 — match provenance ✅
`sourceId: m.sourceId || m.source || null` (the real export keys it `m.source`). Pin: **0/1,432 migrated matches null sourceId** when the source provided one (1,432 provided).

## M3 — identity provenance ✅
`sourceIds: t.sourceIds ? t.sourceIds.slice() : (t.sourceId ? [t.sourceId] : (t.source ? [t.source] : []))`. Pin: **0/792 migrated identities with empty sourceIds** (`MODEL.teams`, `BP-TEAM-PACK`, `BP-TEAM-PACK-v2`, … all carried).

## M3b — multi-league identities count in d3Gate ✅
`d3Gate` now matches coded leagues by competition name too (`canon(comp) === canon(league.name)` / `league.key`), so teams with `leagueCode=null` (multi-league arrays — 242 in the export) still count toward their own league's fitted-gate seasons. Pin: synthetic 2-season store whose only league's teams all carry `leagues:[L1,L2]`, `leagueCode:null` → **d3Gate seasons ≥ 2**.

## Core gate (reproduced verbatim from the auditor's method)
| Check | Result |
|---|---|
| `STORE.deserialize` on the real export | ok:true · rowsIn **2,525 = rowsOut 2,525** |
| Identities / Matches / Venues / Sources | 792 / 1,432 / 86 / 215 ✓ |
| Match row integrity (all 1,432, name/score/date/comp) | **0 mismatched** |
| Migration log | `system/migration — 2525 rows carried` ✓ |
| Booted flow | artifacts `dc-fitted-model` + draw/tiers/markets/records + `dc-gate-validation` (RPL+CZ1); log adds `fitted-migrate` + `dc-gate`; **CSKA–Krylia = fitted** |

## Documentation notes (required, in migration report/ZONES)
- **Top-level `aliases` map (1,078 entries) + `teamStats` (74 records) are derived-not-carried** — identity-level aliases are carried (1,087); the top-level maps are derivable from the carried rows. Stated, not silently dropped (listed in `report.unmapped`).
- **`__DC_GATE__`** is a build-time verdict for seed-era data; it is re-earnable any time via the Replay button — `d3Gate` accepts `replay-validation` artifacts too.
- **Replay league-key hygiene (cosmetic):** codeless competitions report by raw name (`uecl q2`, `Kosovo Superliga`) — noted, no behavior change.

## Full suite state (final file)
smoke **49/49** · R8 13/13 · R9 7/7 · R10 12/12 · **R11 18/18** · R1 ≤3-step ✅ · parity 7/7 · legacy 156/156 · CF grep 0 · fitted-fitted grep 0.
